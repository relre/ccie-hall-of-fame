export interface CCIE {
  name: string;
  certification: string;
  number: number;
  track: string;
  status: string;
  date: string;
  linkedin?: string;
  normalRowNumber?: number;
  activeRowNumber?: number;
} 
